const config = {
  title: "BeerMatch",
  server: {
    // how do we connect?
    port: 9090,
    host: "api.beer.delhaize.com",
    // authentication
    username: "kiosk4",
    passsword: "beer123"
  }
};

export default config;
